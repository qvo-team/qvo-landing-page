<div class="code">
    <!-- Nav tabs -->
    <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" class="active tit_tab"><a href="#suscripciones" aria-controls="suscripciones" role="tab" data-toggle="tab">SUSCRIPCIONES</a>
        </li>
        <li role="presentation" class="tit_tab"><a href="#one_click" aria-controls="one_click" role="tab" data-toggle="tab">ONE CLICK</a>
        </li>
        <li role="presentation" class="tit_tab"><a href="#cobros" aria-controls="cobros" role="tab" data-toggle="tab">COBROS</a>
        </li>

    </ul>

    <!-- Tab panes -->
    <div class="tab-content">

        <div role="tabpanel" class="tab-pane fade in active" id="suscripciones">
<pre class="line-numbers">
<code class=" language-javascript">function test() {
  var hello = "Hello World!"
  if(hello && true) {
    console.log(hello);
  } else {
    console.log('Go fuck yourself.');
  }
}</code>
</pre>
            </div>


            <div role="tabpanel" class="tab-pane fade" id="one_click">

<pre class="line-numbers">
<code class="language-ruby">def destroy
  if @card.blank?
    return render_model_not_found_error('card')
  end

  was_default_card = false

  if @customer.default_payment_method && @customer.default_payment_method.id == @card.id
    was_default_card = true
  end

  Event.create(
    event_type: 'customer.card.deleted',
    data: {card: @card},
    commerce: @current_commerce
  )

  @card.update_attribute :status, 'deleted'

  check_other_cards_and_set_default if was_default_card

  head :no_content
end</code>
</pre>
            </div>
            <div role="tabpanel" class="tab-pane fade" id="cobros">

<pre class="line-numbers">
<code class="language-javascript">function test() {
  alert("Hello world!");
}</code>
</pre>

        </div>
    </div>
</div>